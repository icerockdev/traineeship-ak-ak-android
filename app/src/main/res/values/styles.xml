<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/backgroundWindow</item>

        <item name="android:textAppearanceMedium">@style/CodeFont</item>
        <item name="colorControlNormal">@color/colorButtonHome</item>
    </style>

    <style name="AddImageDialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">false</item>
    </style>

    <style name="CodeFont" parent="@android:style/TextAppearance.Medium">

        <item name="android:textColor">#212121</item>
        <item name="android:typeface">normal</item>
    </style>

</resources>
